<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.HealthGuidanceMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.HealthGuidance">
        SELECT * FROM health_guidance WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.HealthGuidance">
        SELECT * FROM health_guidance
        <where>
            deleted = 0
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="indicator != null and indicator != ''">
                AND indicator = #{indicator}
            </if>
            <if test="isRead != null">
                AND is_read = #{isRead}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO health_guidance (user_id, type, indicator, content, is_read, deleted)
        VALUES (#{userId}, #{type}, #{indicator}, #{content}, #{isRead}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.HealthGuidance">
        UPDATE health_guidance
        <set>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
            <if test="indicator != null and indicator != ''">
                indicator = #{indicator},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="isRead != null">
                is_read = #{isRead},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE health_guidance SET deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectRule" resultType="int">
        select count(*) from health_guidance where user_Id=#{userId} and indicator=#{indicator}  AND create_time >= #{startOfDay} AND create_time &lt; #{endOfDay} and deleted=0
    </select>

</mapper>
