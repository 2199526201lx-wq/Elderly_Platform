<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.UserMapper">
    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.User">
        SELECT * FROM user WHERE  id =#{id} AND deleted=0
    </select>

    <select id="selectByPhone" resultType="com.example.elderly_Platform.core.entity.User">
        SELECT * FROM user WHERE  phone =#{phone} AND deleted=0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT into user (phone, password, real_name, gender, birth_date, height, avatar,
                          emergency_contact, member_level, points, status, role, create_time, update_time, deleted)
        VALUES (#{phone},#{password},#{realName},#{gender},#{birthDate},#{height},#{avatar},#{emergencyContact},#{memberLevel},#{points},#{status},#{role},#{createTime},#{updateTime},0)
    </insert>

    <update id="deductPoints">
        UPDATE user SET points = points - #{amount} WHERE id = #{id}
    </update>

    <update id="addPoints">
        UPDATE user SET points = points + #{amount} WHERE id = #{id}
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE user SET deleted = 1 WHERE id = #{id}
    </update>

    <!-- 动态条件查询用户 -->
    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.User">
        SELECT * FROM user
        <where>
            deleted = 0
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="realName != null and realName != ''">
                AND real_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="memberLevel != null and memberLevel != ''">
                AND member_level = #{memberLevel}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 统计会员总数（role='MEMBER' 且未删除） -->
    <select id="countMembers" resultType="int">
        SELECT COUNT(*) FROM user WHERE role = 'MEMBER' AND deleted = 0
    </select>

    <!-- 统计今日新增会员 -->
    <select id="countMembersToday" resultType="int">
        SELECT COUNT(*) FROM user
        WHERE role = 'MEMBER' AND deleted = 0
          AND DATE(create_time) = CURDATE()
    </select>

    <!-- 动态更新（只更新非空字段） -->
    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.User">
        UPDATE user
        <set>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="realName != null and realName != ''">
                real_name = #{realName},
            </if>
            <if test="gender != null and gender != ''">
                gender = #{gender},
            </if>
            <if test="birthDate != null">
                birth_date = #{birthDate},
            </if>
            <if test="height != null">
                height = #{height},
            </if>
            <if test="avatar != null and avatar != ''">
                avatar = #{avatar},
            </if>
            <if test="emergencyContact != null and emergencyContact != ''">
                emergency_contact = #{emergencyContact},
            </if>
            <if test="memberLevel != null and memberLevel != ''">
                member_level = #{memberLevel},
            </if>
            <if test="points != null">
                points = #{points},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

</mapper>