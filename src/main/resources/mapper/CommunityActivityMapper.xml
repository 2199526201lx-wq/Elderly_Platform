<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.CommunityActivityMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.CommunityActivity">
        SELECT * FROM community_activity WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.CommunityActivity">
        SELECT * FROM community_activity
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="location != null and location != ''">
                AND location = #{location}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectForMember" resultType="com.example.elderly_Platform.core.entity.CommunityActivity">
        SELECT * FROM community_activity
        <where>
            deleted = 0
            AND status != '草稿'
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO community_activity (title, cover_url, content, location,
                                       registration_start_time, registration_end_time,
                                       activity_start_time, activity_end_time,
                                       max_participants, current_participants, status, deleted)
        VALUES (#{title}, #{coverUrl}, #{content}, #{location},
                #{registrationStartTime}, #{registrationEndTime},
                #{activityStartTime}, #{activityEndTime},
                #{maxParticipants}, #{currentParticipants}, #{status}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.CommunityActivity">
        UPDATE community_activity
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="coverUrl != null">
                cover_url = #{coverUrl},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="location != null and location != ''">
                location = #{location},
            </if>
            <if test="registrationStartTime != null">
                registration_start_time = #{registrationStartTime},
            </if>
            <if test="registrationEndTime != null">
                registration_end_time = #{registrationEndTime},
            </if>
            <if test="activityStartTime != null">
                activity_start_time = #{activityStartTime},
            </if>
            <if test="activityEndTime != null">
                activity_end_time = #{activityEndTime},
            </if>
            <if test="maxParticipants != null">
                max_participants = #{maxParticipants},
            </if>
            <if test="currentParticipants != null">
                current_participants = #{currentParticipants},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE community_activity SET deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectForUpdate" resultType="com.example.elderly_Platform.core.entity.CommunityActivity">
        select * from community_activity where id=#{id} and deleted=0 FOR UPDATE
    </select>

    <update id="incrementCount" parameterType="com.example.elderly_Platform.core.entity.CommunityActivity">
        update community_activity
        set current_participants=current_participants+1
        where id=#{id} and current_participants &lt; max_participants
    </update>
</mapper>
