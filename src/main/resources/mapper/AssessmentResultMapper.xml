<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.AssessmentResultMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.AssessmentResult">
        SELECT * FROM assessment_result WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.AssessmentResult">
        SELECT * FROM assessment_result
        <where>
            deleted = 0
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="questionnaireId != null">
                AND questionnaire_id = #{questionnaireId}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO assessment_result (user_id, questionnaire_id, answers, rule_score, ai_score, ai_suggestion, deleted)
        VALUES (#{userId}, #{questionnaireId}, #{answers}, #{ruleScore}, #{aiScore}, #{aiSuggestion}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.AssessmentResult">
        UPDATE assessment_result
        <set>
            <if test="answers != null">
                answers = #{answers},
            </if>
            <if test="ruleScore != null">
                rule_score = #{ruleScore},
            </if>
            <if test="aiScore != null">
                ai_score = #{aiScore},
            </if>
            <if test="aiSuggestion != null">
                ai_suggestion = #{aiSuggestion},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE assessment_result SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>
