<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.HealthRecordMapper">
    <select id="selectHealthyRecordById" resultType="com.example.elderly_Platform.core.entity.HealthRecord">
        SELECT * FROM health_record WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insertHealthyRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO health_record (
            user_id, systolic, diastolic, blood_sugar, heart_rate,
            weight, bmi, memo, recorded_time, create_time, update_time, deleted
        ) VALUES (
            #{userId}, #{systolic}, #{diastolic}, #{bloodSugar}, #{heartRate},
            #{weight}, #{bmi}, #{memo}, #{recordedTime}, #{createTime}, #{updateTime}, 0
        )
    </insert>

    <update id="updateHealthyRecordById" parameterType="com.example.elderly_Platform.core.entity.HealthRecord">
        UPDATE health_record
        <set>
            <if test="systolic != null">
                systolic = #{systolic},
            </if>
            <if test="diastolic != null">
                diastolic = #{diastolic},
            </if>
            <if test="bloodSugar != null">
                blood_sugar = #{bloodSugar},
            </if>
            <if test="heartRate != null">
                heart_rate = #{heartRate},
            </if>
            <if test="weight != null">
                weight = #{weight},
            </if>
            <if test="bmi != null">
                bmi = #{bmi},
            </if>
            <if test="memo != null and memo != ''">
                memo = #{memo},
            </if>
            <if test="recordedTime != null">
                recorded_time = #{recordedTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteHealthyRecordById">
        UPDATE health_record SET deleted = 1 WHERE id = #{id}
    </update>

    <!-- 动态条件查询健康记录 -->
    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.HealthRecord">
        SELECT * FROM health_record
        <where>
            deleted = 0
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="systolic != null">
                AND systolic = #{systolic}
            </if>
            <if test="diastolic != null">
                AND diastolic = #{diastolic}
            </if>
            <if test="recordedTime != null">
                AND recorded_time &gt;= #{recordedTime}
            </if>
        </where>
        ORDER BY recorded_time DESC
    </select>

    <!-- 查询最近 N 个月健康数据统计 -->
    <select id="selectStats" resultType="com.example.elderly_Platform.core.vo.HealthRecordVO">
        SELECT
            AVG(systolic) AS systolic_avg,
            MAX(systolic) AS systolic_max,
            MIN(systolic) AS systolic_min,
            AVG(diastolic) AS diastolic_avg,
            MAX(diastolic) AS diastolic_max,
            MIN(diastolic) AS diastolic_min,
            AVG(blood_sugar) AS blood_sugar_avg,
            MAX(blood_sugar) AS blood_sugar_max,
            MIN(blood_sugar) AS blood_sugar_min,
            AVG(heart_rate) AS heart_rate_avg,
            MAX(heart_rate) AS heart_rate_max,
            MIN(heart_rate) AS heart_rate_min,
            AVG(bmi) AS bmi_avg,
            MAX(bmi) AS bmi_max,
            MIN(bmi) AS bmi_min
        FROM health_record
        WHERE user_id = #{userId}
          AND recorded_time >= #{startTime}
          AND deleted = 0
    </select>

</mapper>