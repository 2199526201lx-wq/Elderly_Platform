<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.MessageMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.Message">
        SELECT * FROM message WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.Message">
        SELECT * FROM message
        <where>
            deleted = 0
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
            <if test="isRead != null">
                AND is_read = #{isRead}
            </if>

        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (user_id, title, content, type, is_read, deleted)
        VALUES (#{userId}, #{title}, #{content}, #{type}, #{isRead}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.Message">
        UPDATE message
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="type != null and type != ''">
                type = #{type},
            </if>
            <if test="isRead != null">
                is_read = #{isRead},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE message SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>
