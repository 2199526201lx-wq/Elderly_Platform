<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.AppointmentMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.Appointment">
        SELECT * FROM appointment WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.Appointment">
        SELECT * FROM appointment
        <where>
            deleted = 0
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="slotId != null">
                AND slot_id = #{slotId}
            </if>
            <if test="packageId != null">
                AND package_id = #{packageId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC, id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO appointment (user_id, slot_id, package_id, status, report_url, original_filename,
                                 report_upload_time, upload_admin_id, deleted)
        VALUES (#{userId}, #{slotId}, #{packageId}, #{status}, #{reportUrl}, #{originalFilename},
                #{reportUploadTime}, #{uploadAdminId}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.Appointment">
        UPDATE appointment
        <set>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="reportUrl != null">
                report_url = #{reportUrl},
            </if>
            <if test="originalFilename != null">
                original_filename = #{originalFilename},
            </if>
            <if test="reportUploadTime != null">
                report_upload_time = #{reportUploadTime},
            </if>
            <if test="uploadAdminId != null">
                upload_admin_id = #{uploadAdminId},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 统计今日预约数 -->
    <select id="countToday" resultType="int">
        SELECT COUNT(*) FROM appointment WHERE deleted = 0 AND DATE(create_time) = CURDATE()
    </select>

    <!-- 统计待完成预约（待确认 + 已确认） -->
    <select id="countPending" resultType="int">
        SELECT COUNT(*) FROM appointment
        WHERE deleted = 0 AND status IN ('待确认', '已确认')
    </select>


    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE appointment SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>
