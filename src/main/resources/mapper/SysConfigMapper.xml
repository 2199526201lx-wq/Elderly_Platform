<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.SysConfigMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.SysConfig">
        SELECT * FROM sys_config WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.SysConfig">
        SELECT * FROM sys_config
        <where>
            deleted = 0
            <if test="configKey != null and configKey != ''">
                AND config_key = #{configKey}
            </if>
            <if test="configValue != null">
                AND config_value = #{configValue}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_config (config_key, config_value, description, deleted)
        VALUES (#{configKey}, #{configValue}, #{description}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.SysConfig">
        UPDATE sys_config
        <set>
            <if test="configValue != null">
                config_value = #{configValue},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE sys_config SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>
