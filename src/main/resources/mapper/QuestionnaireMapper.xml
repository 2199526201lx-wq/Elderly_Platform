<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.elderly_Platform.core.mapper.QuestionnaireMapper">

    <select id="selectById" resultType="com.example.elderly_Platform.core.entity.Questionnaire">
        SELECT * FROM questionnaire WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByCondition" resultType="com.example.elderly_Platform.core.entity.Questionnaire">
        SELECT * FROM questionnaire
        <where>
            deleted = 0
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO questionnaire (title, description, status, total_score, pass_score, grade_rules, deleted)
        VALUES (#{title}, #{description}, #{status}, #{totalScore}, #{passScore}, #{gradeRules}, 0)
    </insert>

    <update id="updateById" parameterType="com.example.elderly_Platform.core.entity.Questionnaire">
        UPDATE questionnaire
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="totalScore != null">
                total_score = #{totalScore},
            </if>
            <if test="passScore != null">
                pass_score = #{passScore},
            </if>
            <if test="gradeRules != null">
                grade_rules = #{gradeRules},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById">
        UPDATE questionnaire SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>
